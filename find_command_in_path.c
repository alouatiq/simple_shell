#include "shell.h"

/**
 * find_command_in_path - Searches for a command in the PATH
 * @command: The command to search for
 *
 * Task 3: Simple Shell 0.3
 *
 * Return: Full path to the command if found, NULL if not found.
 */
char *find_command_in_path(char *command)
{
    /* Code to search for the command in the PATH */
    /* This involves parsing the PATH environment variable and checking each directory */
    
    /* Return the full path or NULL */
}
